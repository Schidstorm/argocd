repositories:
 - name: prometheus-community
   url: https://prometheus-community.github.io/helm-charts

releases:
- name: minecraft
  namespace: minecraft
  createNamespace: true
  chart: ./chart
  version: 1.0.0
  values:
    - ./values.yaml